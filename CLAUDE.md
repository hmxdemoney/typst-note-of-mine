# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# 本目录说明

- 本目录是**个人笔记库**，使用 **Typst**（一种排版语言，类似 LaTeX，文件后缀为 `.typ`）编写排版。
- 笔记全部为**纯文本文件**，使用**中文**撰写。
- **不配置任何运行环境**：不安装依赖、不构建、不编译。所有文件直接用文本编辑器或查看器打开即可，不要引入任何工具链配置文件（如 `package.json`、`Cargo.toml`、`typst.toml` 等）。
- 维护约束：
  - 新建或修改的笔记内容默认使用中文。
  - 只做纯文本编辑；如需渲染/预览 Typst 文档，用户会自行处理，不需要在此仓库配置环境。
